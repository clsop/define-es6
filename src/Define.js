'use strict';

import HttpRequest from '../bower_components/request-es6/src/HttpRequest';

let define = global.define || (id, dependencies, factory) => {
	// TODO: parse params
	// TODO: id resolve
	// TODO: deps resolve
};

export define;